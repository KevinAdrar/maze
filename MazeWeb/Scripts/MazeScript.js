document.onload = function () {
    document.addEventListener("keydown", function (event) {
        if (event.keyCode = 37)/*Left*/ {
            //37: {x: 1, y: 0} 
            
        }   
    });
}